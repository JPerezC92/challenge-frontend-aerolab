import { css } from '@emotion/react';
import { SharedStyles } from 'src/modules/shared/theming/sharedStyles/SharedStyles';

export const ElevationDefault = css({
  boxShadow: '0px 2px 8px rgba(0, 0, 0, 0.05)',
});
