import styled from '@emotion/styled';
import { Selector } from 'src/modules/shared/components/base/Selector';

export const SortSelector = styled(Selector)({
  paddingInline: '1.5rem',
  paddingBlock: '.5rem',
  whiteSpace: 'nowrap',
});
